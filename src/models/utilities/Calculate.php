<?php namespace Causey\Utilities;

class Calculate
{
    public static function driverStats(array $input)
    {
        //accept input in any form

        //calculate here, use helper functions if need be

        //output in a way that is useful to any calling function
        //a simple array that can be written to db or json for the caller to be the decider
    }
}